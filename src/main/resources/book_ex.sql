-- 프로젝트에 ojdbc8.jar 연결 한 후 진행 
-- 프로젝트 우클릭 -> Build Path -> Config build path... -> Libraries 탭 -> Classpath에 삽입 -> apply
-- Deployment Assembly -> java build path -> ojdbc8.jar 추가 -> apply
-- 결과 Referenced Libraries에 ojdbc8.jar 보이면 성공

